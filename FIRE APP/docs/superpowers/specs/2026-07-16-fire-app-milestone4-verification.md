# 里程碑 4 验证计划 / Milestone 4 Verification Plan

> **状态**：待执行
> **日期**：2026-07-16
> **关联**：[M4 设计文档](./2026-07-16-fire-app-milestone4-transaction-management-design.md) · [M4 实施计划](../plans/2026-07-16-fire-app-milestone4-transaction-management.md)
> **前置**：M4 八个 Task 已全部实施并提交（commit `09ccc27` → `7bb844a`），自动化验证全绿（8 文件 / 72 测试通过）

---

## §1 验证范围与方法

### 1.1 背景

M4 自动化验证（renderer 72 测试 + shared 107 测试无回归 + 双 tsc 通过）已覆盖纯函数逻辑、组件交互、store 状态机。但以下三类风险是自动化测试盲区：

1. **真实 IPC 集成**：测试 mock 了 dataAccess，真实 sqlite + IPC 跨进程通信未验证
2. **数据精度与余额联动**：真实数据库写入 + 账户余额反向冲销
3. **视觉与交互流畅性**：Tailwind 样式渲染、布局、响应速度

本验证计划以"手动 GUI 交互"组织，对 M4 全部产出文件进行真实环境验证。

### 1.2 验证范围（5 项）

| # | 验证项 | 目的 |
|---|--------|------|
| H-1 | 启动 app，交易页正常加载 | 真实 sqlite + IPC 集成，确认 mock 未掩盖问题 |
| H-2 | 真实新增 1 笔 income + 1 笔 transfer | 数据库真实写入 + 余额真实联动 |
| H-3 | 真实编辑 + 删除各 1 笔 | 软删除 + 余额回滚真实生效 |
| H-4 | 视觉检查：概览卡颜色/布局、筛选区、列表、表单弹窗 | 自动化测试不覆盖视觉 |
| H-5 | 真实筛选 + 排序交互 | UI 响应真实流畅 |

### 1.3 验证方法

| 方法 | 适用 | 执行方 |
|---|---|---|
| 真实 GUI 交互 | 全部 5 项 | 用户本地执行 |
| 选项式对话确认 | 逐项结果收集 | AI 通过 AskUserQuestion |
| 实时修复 | 发现的 bug | AI 修复 + 回归验证 |

### 1.4 修复流程

发现 → 定位根因 → 修复 → 回归验证（pnpm test:desktop + build）→ 独立 commit（消息前缀 `fix(transactions):`）。

### 1.5 环境前置

- **Node.js 20.x LTS**（不要用 Node 22/24，ABI 不匹配）
- **pnpm** ≥ 9
- 启动命令：`pnpm dev`
- 首次 clone 需 `pnpm bootstrap`
- 环境问题诊断：`pnpm check-env`

---

## §2 验证前准备

### 2.1 拉取最新代码

```bash
cd D:\Projects\Fire-APP
git pull
```

确认最新 commit 包含 M4 全部 8 个 Task（`09ccc27` 到 `7bb844a`）。

### 2.2 环境检查

```bash
pnpm check-env
```

如有问题按提示修复（详见 [env-setup.md](../env-setup.md)）。

### 2.3 启动应用

```bash
pnpm dev
```

等待 Electron 窗口打开，确认控制台无报错。

### 2.4 数据准备

**前置条件**：需至少 2 个账户用于 transfer 验证。

- 若 M3 验证时已创建账户，可直接使用
- 若账户不足 2 个，先到"账户管理"页创建（如"招商银行"、"支付宝"）

---

## §3 H-1 启动加载验证

**目的**：真实 sqlite + IPC 集成，确认 mock 未掩盖问题

### 操作步骤

1. 启动 `pnpm dev`
2. 等待 Electron 窗口打开
3. 点击左侧导航"交易记录"

### 预期结果（6 项检查点）

| # | 检查点 | 预期 |
|---|--------|------|
| a | 页面标题 | "交易记录" 显示 |
| b | 新增按钮 | 右上角 "+ 新增交易" 按钮显示 |
| c | 空状态文案 | 无交易时列表显示"暂无交易记录，点击右上角「新增交易」开始记录" |
| d | 概览卡 | 无交易时不渲染（避免显示 3 个 ¥0.00） |
| e | 筛选区 | 5 个控件显示（类型/账户/分类/日期×2 + 重置按钮） |
| f | 控制台 | 无报错 |

### 通过标准

6 项全部符合。

---

## §4 H-2 新增交易验证

**目的**：数据库真实写入 + 余额真实联动
**前置**：已创建至少 2 个账户（如"招商银行"、"支付宝"）

### 操作步骤 A（新增 income）

1. 点 "+ 新增交易"
2. 交易类型选 "收入"
3. 账户选 "招商银行"
4. 金额输入 `1000`
5. 日期选今天
6. 描述输入 "工资"
7. 点 "确定"

### 预期 A（5 项检查点）

| # | 检查点 | 预期 |
|---|--------|------|
| a | 弹窗 | 关闭 |
| b | toast | "交易创建成功" |
| c | 列表 | 出现新交易行（绿色"收入"标签 + `+ ¥1,000.00`） |
| d | 概览卡 | 显示（收入 ¥1,000.00 / 支出 ¥0.00 / 结余 ¥1,000.00） |
| e | 账户页 | 切到账户页，招商银行余额 +1000 |

### 操作步骤 B（新增 transfer）

1. 点 "+ 新增交易"
2. 交易类型选 "转账"
3. 账户选 "招商银行"
4. 目标账户选 "支付宝"（出现"目标账户"下拉）
5. 金额输入 `500`
6. 点 "确定"

### 预期 B（5 项检查点）

| # | 检查点 | 预期 |
|---|--------|------|
| a | 弹窗 + toast | 弹窗关闭 + 成功 toast |
| b | 列表 | 出现转账行（蓝色"转账"标签 + 账户显示"招商银行 → 支付宝"） |
| c | 金额显示 | `¥500.00`（蓝色，无 +/- 符号） |
| d | 概览卡 | 结余不变（transfer 不计入结余） |
| e | 账户页 | 招商银行 -500，支付宝 +500 |

### 通过标准

A + B 全部符合。

---

## §5 H-3 编辑 + 删除验证

**目的**：软删除 + 余额回滚真实生效

### 操作步骤 A（编辑）

1. 点击 income 交易行的 "编辑"
2. 金额从 `1000` 改为 `2000`
3. 点 "确定"

### 预期 A

| # | 检查点 | 预期 |
|---|--------|------|
| a | 弹窗 + toast | 弹窗关闭 + "交易更新成功" |
| b | 列表金额 | 变为 `+ ¥2,000.00` |
| c | 概览卡 | 收入变为 ¥2,000.00 |
| d | 账户页 | 招商银行余额变为 +2000 |

### 操作步骤 B（删除）

1. 点击某交易行的 "删除"
2. 确认弹窗显示"确定删除此交易记录吗？此操作不可撤销。"
3. 点 "确认"

### 预期 B

| # | 检查点 | 预期 |
|---|--------|------|
| a | 弹窗 + toast | 确认弹窗关闭 + "交易已删除" |
| b | 列表 | 该交易行消失 |
| c | 概览卡 | 数值相应减少 |
| d | 账户页 | 余额回滚 |

### 通过标准

A + B 全部符合。

---

## §6 H-4 视觉检查

**目的**：自动化测试不覆盖的视觉验证

### 检查项（8 项）

| # | 检查项 | 预期 |
|---|--------|------|
| 1 | 概览卡布局 | 3 张卡 grid-cols-3 布局，间距均匀 |
| 2 | 收入卡 | 绿点 + "收入"标签 + 金额 |
| 3 | 支出卡 | 红点 + "支出"标签 + 金额 |
| 4 | 结余卡 | 蓝点 + "结余"标签 + 金额（负数时红色文字） |
| 5 | 筛选区 | 5 个控件一行排列 + 重置按钮右对齐 |
| 6 | 列表表头 | 6 列对齐（类型/日期/账户/分类/金额/操作） |
| 7 | 类型标签 | 4 种颜色区分（收入绿/支出红/转账蓝/初始余额紫） |
| 8 | 表单弹窗 | 字段排列整齐，transfer 时"目标账户"下拉出现 |

### 通过标准

8 项全部符合。

---

## §7 H-5 筛选 + 排序交互

**目的**：UI 响应真实流畅

### 操作步骤 A（类型筛选）

1. 类型下拉选 "收入"
2. 观察列表 + 概览卡

**预期 A**：列表只显示收入交易，概览卡数值随筛选变化

### 操作步骤 B（账户筛选 + transfer 双向匹配）

1. 账户下拉选 "招商银行"
2. 观察列表

**预期 B**：显示招商银行作为 source 或 target 的所有交易（含 transfer 转入转出）

### 操作步骤 C（日期筛选）

1. 起始日期选今天
2. 结束日期选今天

**预期 C**：只显示今天的交易

### 操作步骤 D（重置）

1. 点 "重置" 按钮

**预期 D**：5 个筛选全部清空，列表恢复全量

### 操作步骤 E（排序）

1. 排序下拉切 "金额降序"
2. 观察列表

**预期 E**：列表按金额从大到小重排

### 操作步骤 F（无匹配空状态）

1. 起始日期选未来日期（如明天）
2. 观察列表

**预期 F**：列表显示"无匹配交易，试试调整筛选条件"

### 通过标准

A-F 全部符合。

---

## §8 验证结果汇总

> 验证完成后填写本节。

### 8.1 验证结果表

| # | 验证项 | 结果 | 备注 |
|---|--------|------|------|
| H-1 | 启动加载 | 待验证 | |
| H-2 | 新增交易 | 待验证 | |
| H-3 | 编辑 + 删除 | 待验证 | |
| H-4 | 视觉检查 | 待验证 | |
| H-5 | 筛选 + 排序 | 待验证 | |

### 8.2 发现的问题及修复

> 验证过程中发现的 bug 及修复记录。

### 8.3 经验归纳

> 验证完成后的经验总结。
